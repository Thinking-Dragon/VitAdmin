<Page x:Class="VitAdmin.View.ViewAdminModificationStructure"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VitAdmin.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ViewAdminModificationStructure">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="128">
            </RowDefinition>
        </Grid.RowDefinitions>
        <ContentPresenter x:Name="cpListeDepartements"
                          Grid.RowSpan="2"></ContentPresenter>
        <ContentPresenter x:Name="cpApercuDepartementSelectionne"
                          Grid.Column="1"></ContentPresenter>
        <Button Grid.Column="1"
                Grid.Row="1"
                Command="{Binding CmdEquipements}">
            Équipements
        </Button>
    </Grid>
</Page>
